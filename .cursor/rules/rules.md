The project's working notes live in CLAUDE.md at the repository root — architecture,
commands, and the non-obvious constraints that have already cost time once.

Decentraland SDK reference material is installed under `.claude/skills/` (git-ignored).
Reinstall with `npx skills add decentraland/sdk-skills --all` if it is missing.
